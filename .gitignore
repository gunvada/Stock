# API 키가 든 로컬 설정은 절대 커밋 금지 (Secrets로만 주입)
config.json

# 캐시는 클라우드에서 매번 재수집 (레포 비대 방지)
output/cache/

# 로컬 산출물
logs/
__pycache__/
*.pyc
tmp_*.py

# 로컬 Claude 설정 (프로젝트 무관)
.claude/

# 파생 분석 산출물(캐시에서 매 실행 재생성 → 추적 제외, 결론은 dashboard.html에 보존)
output/feature_analysis.csv
output/prior_runup_analysis.csv
