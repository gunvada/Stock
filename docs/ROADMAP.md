# 개선 로드맵 (마스터 체크리스트)

수익 확률·신뢰성을 높이기 위한 점검·보강 항목. 우선순위 **P0(치명적) → P2(안정성)**.
각 항목에 담당 트랙 태그: `[본장]` `[프리마켓]` `[공통]`.

> 트랙별 운영 가이드: [docs/REGULAR.md](REGULAR.md) · [docs/PREMARKET.md](PREMARKET.md)

---

## P0 — 실투자 전 필수

- [~] **워크포워드/구간 안정성 검증** `[본장]` ✅ 1차 2026-06-17 — `walkforward.py`(394거래 6구간 + 거래량버킷). **결과: TP/SL 플랜은 6구간 중 5구간 마이너스(양수구간 17%), 최근 1구간만 +.** 종가청산은 양수구간 83%이나 중앙값≈0(대박 의존). → **현 플랜은 강건한 엣지 아님(장세 의존/최근 편중).** 남은 일: 1~2년 장기 데이터로 확대(현재 3개월 표본).
- [ ] **거래비용·슬리피지 현실화** `[공통]`
  - `COST=2.5%` 고정 → 가격대·거래대금별 차등 스프레드/슬리피지, 갭하락 손절 슬립 반영.
- [x] **리버스 스플릿 탐지·제외 필터** `[본장]` ✅ 2026-06-17
  - 역분할이 가짜 거래량배율/가짜 급등을 만듦(페니주 빈번). 분할 이벤트 종목 제외/플래그.
  - 구현: `scanner.fetch_recent_splits`/`split_exclusion_set`(순수함수) + `pullback_scanner`에서 최근 분할 종목 제외. config `scan.exclude_recent_splits`/`split_lookback_days`/`split_filter_mode`. 테스트 `tests/test_splits.py`.
- [ ] **분봉 백테스트(룩어헤드/체결 정밀화)** `[본장]` `[프리마켓]`
  - TP·SL 같은 날 둘다 터치 시 보수적 손절가정 → 분봉으로 선후 판정. 실제 진입룰(VWAP회복) 재현.
- [ ] **포지션 사이징·리스크 관리 도입** `[공통]`
  - 현재 자금관리 규칙 전무. 트레이드당 리스크%, 동시보유 한도, 일일 손실컷, MDD·파산확률, 켈리 사이징.

## P1 — 신뢰도 향상

- [ ] **전진 페이퍼트레이딩 누적** `[공통]` — 장부(`*_ledger.csv`)를 수 주 실시간 누적, 예측 vs 실제 괴리 측정. (인프라 완비 → 가장 싼 검증)
- [ ] **통계적 유의성 검정** `[공통]` — 부트스트랩 신뢰구간, 동일 유니버스 랜덤선택 대비 초과수익(엣지 진위).
- [ ] **상관·집중 리스크** `[공통]` — 같은 날 픽의 테마/섹터 상관 → 분산 한도.
- [ ] **할트(상승 LULD) 모듈** `[프리마켓]` — 상승 할트→재개 수익률 백테스트(무료 Nasdaq 할트 리스트+분봉). "긍정 신호" 가설을 숫자로 검증(몇 번째 할트까지 안전한지). 재개 반전·할트중 진입불가 비용 반영.
- [ ] **데이터 교차검증 확대** `[본장]` — 백테스트도 Polygon↔Yahoo 교차(현재 단일소스).

## P2 — 코드 견고성·운영

- [ ] **단위테스트 추가** `[공통]` — compute_surge·필터·채점·날짜파싱(이미 1회 크래시 경험). `tests/`.
- [ ] **의존성 버전 고정** `[공통]` — `requirements.txt` `>=` → `==` 핀(현재 pandas 3.0.3 등 최신 자동설치 위험).
- [ ] **에러처리·실패 알림** `[공통]` — 네트워크/빈응답/부분데이터 핸들링, Action 실패 시 알림(예약 실패를 뒤늦게 발견한 전례).
- [ ] **yfinance 신뢰성** `[프리마켓]` — 레이트리밋·간헐실패 재시도. 프리마켓 거래량(야후 미제공) 유료소스 검토.
- [ ] **DST 처리** `[프리마켓]` — 프리마켓 창 EDT 고정 → KST 기준 동적 ET 변환(겨울 1시간 시프트).

---

## 캔들매매 이론 통합 `[본장]` (출처: 드라이브 캔들/파동 교재 → `docs/STRATEGY_NOTES.md`)

> ⚠️ 정합성: 캔들이론은 "깊은 하락 후 다년 베이스 + 캔들신호 → +100~200%" 전략으로,
> 현 거래량폭증 스캐너와 철학이 다름. 1·2는 현 스캐너에 부착 가능, 3·4는 사실상 **새 스캐너**.

- [ ] **과상승 제외 필터** — 저점 대비 +50%/10배↑ 종목 제외(해석제외 #1·#3). (현 스캐너 부착 가능, 다년 저점 필요)
- [ ] **상장기간/캔들수 필터** — 신규상장·바 부족 제외(해석제외 #4). (가벼움, 즉시 가능)
- [x] **캔들 패턴 탐지기** — 양봉스프링/위꼬리양봉/양봉팽이/긴위아래꼬리 작은양봉 등 몸통·꼬리 비율 룰화. ✅ 2026-06-17 (`candle_patterns.py` + `tests/test_candle_patterns.py`, 단일캔들 OHLC 순수룰)
- [x] **캔들 시그널·모양 교차검증** ✅ 2026-06-17 — `candle_verify.py`: 본장 후보를 **오로지 캔들 시그널/모양**으로 재검증(위치·베이스 지표 미사용 — 사용자 정의). 최근 LOOKBACK(5)일 일봉에 상승 캔들(candle_patterns) 출현 시 PASS. daily.yml 장착 + 대시보드 노출. **실측(6/16): 7→3 통과(SNWV·BEEM·AHMA).**
  - (참고) 베이스근처·베이스비율·깊은하락 등 위치 지표는 회원 요청으로 제외.
- [ ] **분할매수·+100/200% 익절·저가하단 손절** 청산 로직 백테스트 옵션화.

## 다중 캔들 패턴 `[본장]`
- [x] **상승펀치·상승다람쥐·양봉팽이군·꼬리군** ✅ 2026-06-17 — `candle_patterns.detect_multi` + 단위테스트. `candle_verify`에서 단일+다중 신호 합산(PASS = 둘 중 하나라도).
- [ ] (후속) 음봉 다중 패턴(하락펀치/하락다람쥐) — 매도/회피용.

## 진행 메모
- 2026-06-17: `[본장]` 리버스 스플릿 필터 구현·머지. 단위테스트 4종 통과. **라이브 daily.yml 정상 완주(success) 확인** — 크래시 없음, 당일 제외 0건(정상).
- 2026-06-17: 드라이브 캔들/파동 교재 정제 → `docs/STRATEGY_NOTES.md` 생성. 캔들이론 통합 항목 등록(위).
- 2026-06-17: `candle_patterns.py`(패턴 탐지기) → `candle_verify.py`(교차검증) 빌드. daily.yml 장착·대시보드 노출.
- 2026-06-17: 교차검증 기준을 회원 요청대로 **'캔들 시그널·모양만'**으로 변경(위치/베이스 지표 제거). 최근 5일 캔들 출현 기준. 실측 7→3(SNWV·BEEM·AHMA). 테스트 16종 통과.
- 2026-06-17: 파동연상 Ⅰ·Ⅱ·Ⅲ 정독 → 추세='구간별 위치'(완전 자동화 불가, 저자 명시) 확인. **추세 분류기(장기이평 근사: 상승/하락/횡보) 추가**하되 PASS 강제 안 함(표시용). 이유: 하락세 '바닥 반등'도 유효 → 일괄 제외 부적절. 테스트 20종 통과.
